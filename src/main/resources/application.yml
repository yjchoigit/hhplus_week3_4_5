spring:
  application:
    name: week-03
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/demoProject
    username: root
    password: test123
#    hikari:
#      transaction-isolation: SERIALIZABLE

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        use_new_id_generator_mappings: true
        format_sql: true
        show_sql: true

jwt:
  secret: your_strong_secret_key_which_is_32_bytes_long
  expiration: 3600000 # 만료 시간 (밀리초 단위)

# JPA 및 H2 데이터베이스 로그 출력
#logging:
#  level:
#    org:
#      springframework:
#        data: debug
#      hibernate: debug
#    com:
#      zaxxer:
#        hikari: debug
#    h2: debug

springdoc:
  packages-to-scan: com.hhplus.hhplus_week3_4_5.ecommerce.controller
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: /
    disable-swagger-default-url: true
    display-request-duration: true
    operations-sorter: alpha


server:
  port: 8080